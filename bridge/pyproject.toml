[project]
name = "kaptain"
version = "0.1.0"
description = "Kaptain — a Kubernetes scheduler extender to plug in ML/LLM placement strategies"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.0",
    "uvicorn[standard]==0.30.6",
]

[tool.uv]
package = false
