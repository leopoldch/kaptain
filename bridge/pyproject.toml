[project]
name = "kaptain"
version = "0.1.0"
description = "Kaptain — the out-of-process decider the Kubernetes scheduler plugin calls"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.0",
    "uvicorn[standard]==0.30.6",
    "prometheus-client==0.21.0",
]

[tool.uv]
package = false

