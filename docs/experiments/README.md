# Experiments

One file per experiment: the question it answers, the design, the guards that make the
numbers mean something, and the outputs. Written before the experiment runs, updated with
what actually happened afterwards.

| Experiment | Question | Status |
|---|---|---|
| [pilot-integration-cost.md](pilot-integration-cost.md) | At equal policy, what does the integration cost — extender against plugin, idle and under load? | Specified, not run |

The runner that executes these lives in [`../../experiments/`](../../experiments/) and does
not exist yet.
